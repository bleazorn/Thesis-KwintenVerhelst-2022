Milestone 2

optimize-let-bindings interpreters en checkers 
	niet geimplementeerd???

Create makefile

Optelling en returning negatives shell
	(module (let ([y -10] [x -5]) y))
	(module (let ([y -10] [x -5]) (+ x y)))


--Compiler Steps
	
uniquify
	ok
sequentialize-let
	ok
normalize-bind
	ok
select-instructions
	ok -- check voor optimalisatie
assign-homes
	ok
uncover-locals
	ok - volgorde locals kan verschillend zijn, ni echt belangrijk
assign-fvars
	ok -- check voor optimalisatie
replace-locations
	ok
flatten-begins
	ok
patch-instructions
	ok -- check voor optimalisatie
implement-fvars
	ok
generalize-cheri-risc-v
	niet ok Voeg manipulatie stack pointer toe

(module
                      (define swap
                        (lambda (x y)
                          (if (< y x)
                              x
                              (let ([z (call swap y x)])
                                z))))
                    (call swap 1 2))
